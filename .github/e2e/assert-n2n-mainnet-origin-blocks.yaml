---
apiVersion: v1
kind: ConfigMap
metadata:
  name: assert-n2n-mainnet-origin-blocks
data:
  daemon.toml: |-
    [source]
    type = "N2N"
    address = ["Tcp", "relays-new.cardano-mainnet.iohk.io:3001"]
    magic = "mainnet"

    [source.finalize]
    max_block_quantity: 5000

    [source.intersect]
    type = "Origin"

    [sink]
    type = "Assert"
    break_on_failure = true
---
apiVersion: batch/v1
kind: Job
metadata:
  name: assert-n2n-mainnet-origin-blocks
  labels:
    app: assert-n2n-mainnet-origin-blocks
spec:
  template:
    metadata:
      labels:
        app: assert-n2n-mainnet-origin-blocks
    spec:
      restartPolicy: "Never"
      containers:
        - name: main
          image: ${TARGET_IMAGE}
          env:
            - name: "RUST_LOG"
              value: "warn"
          resources:
            requests:
              memory: 100Mi
              cpu: 50m
            limits:
              memory: 500Mi
              cpu: 200m
          args:
            - "daemon"
          volumeMounts:
            - mountPath: /etc/oura
              name: oura-config
      volumes:
        - name: oura-config
          configMap:
            name: oura
